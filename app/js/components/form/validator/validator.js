define(["underscore", "when"], function(_, When) {
  var Validator;
  return Validator = (function() {
    function Validator() {}

    Validator.prototype.parseStrategy = function() {
      return console.log("parseStrategy");
    };

    return Validator;

  })();
});
